package com.ttrc.recordtrendsService.mapper;

import com.ttrc.recordtrendsService.dto.RecordTrendsDto;
import com.ttrc.recordtrendsService.entity.RecordTrends;

public class RecordTrendsMapper {

/*
    // Convert User JPA Entity into UserDto
    public static RecordTrendsDto mapToRecordTrendsDto(RecordTrends recordTrends){
        recordTrends.
        RecordTrendsDto recordTrendsDto = RecordTrendsDto.builder()
                .id(recordTrends)
                .build();
    }
    public static UserDto mapToUserDto(User user){
        //UserDto userDto = new UserDto();
        UserDto userDto = UserDto.builder()
                .firstname(user.getFirstname())
                .lastname(user.getLastname())
                .useremail(user.getUseremail())
                .userid(user.getUserid())
                .notes(user.getNotes())
                .isadmin(user.getIsadmin())
                .isactive(user.getIsactive())
                .password(user.getPassword())
                .notes(user.getNotes())
                .build();

        return  userDto;

    }

    // Convert UserDto into User JPA Entity
    public static User mapToUser(UserDto userDto){
        User user = User.builder()
                .firstname(userDto.getFirstname())
                .lastname(userDto.getLastname())
                .useremail(userDto.getUseremail())
                .userid(userDto.getUserid())
                .notes(userDto.getNotes())
                .isadmin(userDto.getIsadmin())
                .isactive(userDto.getIsactive())
                .password(userDto.getPassword())
                .notes(userDto.getNotes())
                .build();

        return user;
    }
*/
}
